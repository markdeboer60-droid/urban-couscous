{
  "name": "ottovisser-partners-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Otto Visser & Partners accountants website – Eleventy static site",
  "scripts": {
    "dev": "eleventy --serve --input=site --output=_site",
    "build": "eleventy --input=site --output=_site"
  },
  "dependencies": {
    "@11ty/eleventy-plugin-rss": "^3.0.0",
    "eleventy-plugin-sitemap": "^1.0.1",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
